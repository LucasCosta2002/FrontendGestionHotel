@inherits LayoutComponentBase

<div class="page ">
    <div class="bg-primario vh-100 p-3 d-flex flex-column justify-content-between w-auto">
        <NavMenu />
    </div>


    <main class="bg-fondo vh-100 p-3 container overflow-auto">
        <div class="container overflow-hidden">
           
            @for (int r = 0; r < 9; r++)
            {
                <div class="row mb-4 gap-4">

                    @for (int c = 0; c < 5; c++)
                    {
                        int habi = 101;
                        @for(int hab = 0; hab < 45; hab++)
                        {
                           habi++;
                        }
                        <div class="col-lg-2 col-md-12 bg-white border-radius shadow p-0">
                            <div class="row gx-1 justify-content-between mb-4">
                                <div class="text-uppercase text-white text-center border-radius-top fw-bold pb-2 bg-verde-card">
                                    <p class="mb-1 "><u> @habi</u></p>
                                    <p class="mb-0">Disponible</p>
                                </div>
                                <div class="px-2 pb-2 text-center">
                                    <p class="fw-bold gris-claro m-0 py-4 "></p>
                                    <button class="btn btn-secondary p-1 text-uppercase fw-bold font-xs">Ver habitacion</button>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            }
             
        </div>
    </main>
</div>

@code {
        public int habitacion = 101;
}

@*
            @for (int i = 0; i < 10; i++)
            {
                    <div class="col-lg-2 col-md-12 bg-white border-radius shadow p-0">
                <div class="row gx-1 justify-content-between mb-4">
                        <div class="text-uppercase text-white text-center border-radius-top fw-bold pb-2 bg-lila-card">
                            <p class="mb-1 "><u>103</u></p>
                            <p class="mb-0">Reparacion</p>
                        </div>
                        <div class="px-2 pb-2 text-center">
                            <p class="fw-bold gris-claro m-0 py-4 ">Humedad</p>
                            <button class="btn btn-secondary p-1 text-uppercase fw-bold font-xs">Ver habitacion</button>
                        </div>
                    </div>
                </div>

            }*@


@*  <div class="row">
                    @for (int y = 0; y < 5; y++)
                    {
                        // Cálculo de la fila actual
                        int columna = y % totalColumnas; // Cálculo de la columna actual

                        <div class="col-lg-2 col-md-12 bg-white border-radius shadow p-0">
                            <div class="text-uppercase text-white text-center border-radius-top fw-bold pb-2 bg-rojo-card">
                                <p class="mb-1 "><u>101</u></p>
                                <p class="mb-0">Ocupada</p>
                            </div>
                            <div class="px-2 pb-2 text-center">
                                <p class="fw-bold gris-claro m-0 py-4 ">Joaquin Zamora</p>
                                <button class="btn btn-secondary p-1 text-uppercase fw-bold font-xs">Ver habitacion</button>
                            </div>
                        </div>

                        if (columna == totalColumnas - 1)
                        {
                            <div class="row"></div>
                        }
                    }
                </div>*@